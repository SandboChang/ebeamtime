# Extraction Provenance

This repository was filtered from `SandboChang/scgds` at source commit
`1c36052a4c13de9e769a09d9cf2577cd4fa59a79` with `git-filter-repo` 2.47.0.

The extraction retained the original license and all commits that introduced
`scgds/ebeamtime` or its focused tests. Paths were normalized to
`src/ebeamtime` and `tests`. Retained non-empty commit mappings are recorded in
`provenance/scgds-commit-map.txt`.
