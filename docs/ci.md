# Cross-platform CI status

Hosted installation from the private `gdsdiff` repository is intentionally not
configured because it would require distributing a repository credential.
Until both projects are public or `gdsdiff` is released to PyPI, qualification
is run locally from sibling checkouts on each available platform. An active
three-OS workflow should be enabled before the first public release.
